#!/usr/bin/env python
"""
Backwards-compatible setup.py for editable installs and older pip versions.
All configuration is in pyproject.toml.
"""
from setuptools import setup

if __name__ == "__main__":
    setup()
